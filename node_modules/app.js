const express = require("express")
const app=express()
const PORT = 3000
app.get('/',(req,res) => {

    res.send('ahned adel mohamed fouad wahsh - sec1')


})
app.listen(PORT,()=>{
    console.log('example app listening at http;//localhost:${PORT}')
})